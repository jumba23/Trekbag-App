import Button from "./Button";

const ButtonGroup = () => {
  return (
    <section className="button-group">
      <Button />
      <Button />
      <Button />
      <Button />
    </section>
  );
};

export default ButtonGroup;
